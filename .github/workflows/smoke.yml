name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke-test:
    name: smoke test (${{ matrix.os }}) on py ${{ matrix.python-version }} via ${{ matrix.install-source }}
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.13", "3.12", "3.11"]
        install-source: [local, pypi]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        if: matrix.install-source == 'local'
        uses: snok/install-poetry@76e04a911780d5b312d89783f7b1cd627778900a # v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Build Wheel
        if: matrix.install-source == 'local'
        run: poetry build

      - name: Install local Wheel
        if: matrix.install-source == 'local'
        run: pip install dist/*.whl

      - name: Install from PyPI
        if: matrix.install-source == 'pypi'
        run: pip install ansel-cli

      - name: Smoke Test - Version
        run: ansel version

      - name: Smoke Test - Builtins
        run: ansel -f tests/integration_config.yaml builtins

      - name: Smoke Test - Templates
        run: ansel -f tests/integration_config.yaml templates
